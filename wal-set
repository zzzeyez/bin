#!/usr/bin/env bash

sassc --style expanded "${HOME}/scripts/pollen/scss/style.scss" > "${HOME}/scripts/pollen/style.css" &&
sassc --style expanded "${HOME}/uber/Pecan/scss/style.scss" > "${HOME}/uber/Pecan/style.css" &&
refresh_ubersicht
